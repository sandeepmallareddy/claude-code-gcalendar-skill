# Google Calendar Skill - Environment Variables
# Copy this file to .env and fill in your values
#
# NOTE: Alternatively, you can create a credentials.json file in the project root:
# {
#   "client_id": "your-client-id",
#   "client_secret": "your-client-secret",
#   "redirect_uris": ["http://localhost:3000/callback"]
# }

# OAuth 2.0 Credentials
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-client-id-here
GOOGLE_CLIENT_SECRET=your-client-secret-here

# OAuth Redirect URI
# Must match the redirect URI in your Google Cloud OAuth credentials
GOOGLE_REDIRECT_URI=http://localhost:3000/callback

# Default Calendar ID (usually 'primary')
GCALENDAR_DEFAULT_CALENDAR=primary

# Optional: Output format (text, json)
# GCALENDAR_OUTPUT_FORMAT=text

# Optional: Default timezone
# GCALENDAR_TIMEZONE=America/New_York
